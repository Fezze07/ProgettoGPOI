// Definisce le route REST per il dominio "stock" (es. GET /api/stock, GET /api/stock/:symbol).
// Monta i middleware di validazione e collega ogni route al metodo corrispondente del stockController.
// Punto di registrazione di tutte le sotto-route relative ai dati azionari.
