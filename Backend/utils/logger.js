// Fornisce un sistema di logging centralizzato per errori e messaggi di debug.
// Wrappa console.log/error aggiungendo livelli (INFO, WARN, ERROR) e timestamp a ogni messaggio.
// Può essere esteso per scrivere su file o inviare log a servizi di monitoraggio esterni.
